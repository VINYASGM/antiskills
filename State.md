# State Deprecation Notice

This file is deprecated. Please run the dynamic status command to view the live status of active task progress:

```bash
node bin/veyra.js status
```

Or for JSON output:

```bash
node bin/veyra.js status --json
```
